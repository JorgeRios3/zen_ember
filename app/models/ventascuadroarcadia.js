import Model from 'ember-data/model';
import DS from 'ember-data';

const {
	attr
} = DS;

export default Model.extend({
	mes: attr('string'),
	a2000: attr('formattedint', { defaultValue: 0 }),
	a2001: attr('formattedint', { defaultValue: 0 }),
	a2002: attr('formattedint', { defaultValue: 0 }),
	a2003: attr('formattedint', { defaultValue: 0 }),
	a2004: attr('formattedint', { defaultValue: 0 }),
	a2005: attr('formattedint', { defaultValue: 0 }),
	a2006: attr('formattedint', { defaultValue: 0 }),
	a2007: attr('formattedint', { defaultValue: 0 }),
	a2008: attr('formattedint', { defaultValue: 0 }),
	a2009: attr('formattedint', { defaultValue: 0 }),
	a2010: attr('formattedint', { defaultValue: 0 }),
	a2011: attr('formattedint', { defaultValue: 0 }),
	a2012: attr('formattedint', { defaultValue: 0 }),
	a2013: attr('formattedint', { defaultValue: 0 }),
	a2014: attr('formattedint', { defaultValue: 0 }),
	a2015: attr('formattedint', { defaultValue: 0 }),
	a2016: attr('formattedint', { defaultValue: 0 }),
	a2017: attr('formattedint', { defaultValue: 0 }),
	a2018: attr('formattedint', { defaultValue: 0 }),
	a2019: attr('formattedint', { defaultValue: 0 }),
	a2020: attr('formattedint', { defaultValue: 0 }),
	a2021: attr('formattedint', { defaultValue: 0 }),
});
